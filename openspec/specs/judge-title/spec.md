## Purpose

Set the HTML `<title>` to "Hero Judge" for correct PWA display name when installed.

## Requirements

### Requirement: HTML title matches PWA brand

The `<title>` in `index.html` SHALL be set to "Hero Judge".

#### Scenario: PWA installs with correct name
- **WHEN** the user installs the judge PWA
- **THEN** the installed app SHALL display "Hero Judge" as its name
